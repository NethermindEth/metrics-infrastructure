global:
  scrape_interval:     15s
  evaluation_interval: 15s

# Nethermind metrics arrive via remote-write from Grafana Alloy
# (see --web.enable-remote-write-receiver), so no scrape jobs are needed here.
